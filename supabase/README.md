# /supabase

Supabase project assets: schema migrations, RLS policies, storage config, and
Edge Functions (analyze, generate-outfit, stylist-chat).

Built in Prompt 3 (schema + RLS + types) and Prompt 4 (Edge Functions).
See /docs/Refined_MVP_Structure.md §7 for the data model.
