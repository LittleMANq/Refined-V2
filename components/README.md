# /components

Shared, RTL-aware UI components: PillButton (primary/secondary), Card (soft),
GarmentSlot (3:4 portrait), Hairline divider, ScreenHeader, Label/Text, and the
reveal-motion helper.

Built in Prompt 2 (design system). Every screen imports from here and never
re-defines tokens. See CLAUDE.md §4.
